#ifndef NODE_H
#define NODE_H

#include <vector>

//Defines a structure called Node which has an x and y value, used to represent spaces within the map
struct Node
{
	int x, y, cost;
};

#endif
